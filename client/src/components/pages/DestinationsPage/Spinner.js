import './Spinner.css'

// const Spinner = (size) => <div className="kinetic"></div>
const Spinner = ({ size }) => {
    const style = { width: size = { size } }
    return < div className="kinetic" style={style} ></div>
}

export default Spinner